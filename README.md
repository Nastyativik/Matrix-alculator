# Калькулятор матриц  
**Бодягина А., ИС-323** 💾

Простая консольная программа на языке **Go**, позволяющая выполнять основные операции с квадратными матрицами размером **2×2** и **3×3**.

---

## 📌 Что умеет программа?

1. **Сложение матриц**
2. **Вычитание матриц**
3. **Умножение матрицы на число**
4. **Умножение двух матриц**

Все матрицы — **квадратные**, размер выбирается пользователем (только 2 или 3).

---

## 🧠 Как работает программа?

### 1. Главное меню
При запуске программа выводит меню:
**Выберите операцию:**

**Сложение матриц**
**Вычитание матриц**
**Умножение матрицы на число**
**Умножение матриц**
**Ваш выбор:**
Пользователь вводит число от **1 до 4**.  
Если введено другое значение — программа сообщает: `Неверный выбор` и завершается.

---

### 2. Ввод размера матрицы
Для операций **1, 2, 3 и 4** программа сначала спрашивает:

Введите размер матрицы **(2 или 3)**:

Поддерживаются **только значения 2 и 3**.  

При вводе любого другого числа — выводится ошибка, и функция завершается.

---

### 3. Ввод элементов матриц
После выбора размера программа запрашивает элементы матриц **построчно**.

Пример ввода матрицы **2×2**:

Введите элементы матрицы A построчно:
1 2
3 4

Каждая строка вводится **через пробел**, всего должно быть **ровно `n` чисел в `n` строках**.

Аналогично вводится вторая матрица (для операций 1, 2 и 4).

---

### 4. Выполнение операции и вывод результата
Программа выполняет выбранную операцию и выводит результат с **двумя знаками после запятой**.

Пример результата сложения:
6.00 8.00
10.00 12.00

---

## 📌 Основные функции

**main()**
Назначение: Точка входа в программу. Выводит меню, считывает выбор пользователя и вызывает соответствующую функцию.
Особенности:
Поддерживает 4 операции с матрицами.
При неверном выборе выводит сообщение "Неверный выбор".

**readMatrix()**
Назначение: Интерактивный ввод квадратной матрицы размером n × n с клавиатуры.
Параметры:
name — имя матрицы для отображения в приглашении (например, "A" или "B");
n — размер матрицы (только 2 или 3).
Возвращает: Заполненную матрицу типа [][]float64.
Особенности:
Создаёт матрицу фиксированного размера n × n.
Считывает элементы построчно через fmt.Scan.
Не содержит встроенной обработки ошибок ввода (только базовая проверка размера в вызывающих функциях).

**printMatrix()**
Назначение: Выводит матрицу в консоль.
Параметры:
m — матрица для вывода.
Особенности:
Каждый элемент отображается с двумя знаками после запятой (%.2f).
Элементы в строке разделены пробелом.
После каждой строки выводится перевод на новую строку.

**addMatrices()**
Назначение: Выполняет сложение двух матриц одинакового размера (2×2 или 3×3).
Особенности:
Запрашивает у пользователя размер матрицы (только 2 или 3).
Считывает две матрицы: A и B.
Выполняет поэлементное сложение: result[i][j] = a[i][j] + b[i][j].
Выводит результат с подписью "Результат сложения:".

**subtractMatrices()**
Назначение: Выполняет вычитание двух матриц одинакового размера.
Особенности:
Аналогична addMatrices, но выполняет: result[i][j] = a[i][j] - b[i][j].
Выводит результат с подписью "Результат вычитания:".

**multiplyByScalar()**
Назначение: Умножает матрицу на скаляр (число).
Особенности:
Запрашивает размер матрицы и саму матрицу A.
Затем запрашивает число-множитель.
Выполняет: result[i][j] = matrix[i][j] * scalar.
Выводит результат с подписью "Результат умножения на число:".

**multiplyMat()**
Назначение: Умножает две квадратные матрицы одинакового размера.
Параметры: нет (всё запрашивается внутри функции).
Особенности:
Запрашивает размер (2 или 3), затем матрицы A и B.
Использует тройной вложенный цикл по стандартному правилу умножения матриц:
result[i][j] += a[i][k] * b[k][j]
Работает только для квадратных матриц одинаковой размерности.
Выводит результат с подписью "Результат умножения матриц:".

